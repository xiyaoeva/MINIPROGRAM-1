<view class="page">
  <view class="card" wx:if="{{job}}">
    <view class="title">{{job.title}}</view>
    <view class="meta">发布日期: {{job.dateString}}</view>
    <view class="section-title">标签</view>
    <view class="tag-list">
      <view class="tag" wx:for="{{job.tags}}" wx:for-item="tag" wx:key="tag">{{tag}}</view>
    </view>
    <view class="section-title">前往文章</view>
    <view class="link" bindtap="openLink"></view>
    <button class="button-secondary" bindtap="copyLink">前往文章</button>
  </view>
  <view class="placeholder" wx:else>暂无信息</view>
</view>
